'use client'
import { useDispatch } from "react-redux";
import { AddTabDataRequest, AddTabDataSuccess, AddTabDataError} from "../reducers/tabReducer";
import { TabState } from "../interfaces/tabInterface";
import { AppDispatch } from "../store";

export const addTab = ()=> (dispatch:AppDispatch) =>{
   
    
    
}